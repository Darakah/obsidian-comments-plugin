import type { CommentsSettings } from './types'

export const VIEW_TYPE_OB_COMMENTS = 'ob_comments';

export const DEFAULT_SETTINGS: CommentsSettings = {
    SHOW_RIBBON: true,
    DEFAULT_COLOR: '#b30202',
    DEFAULT_BACKGROUND_COLOR: '#FFDE5C'
}